{
  "builders": [
    {
      "boot_command": "sudo -i <enter>localectl set-keymap fr<enter>",
      "boot_wait": "{{user `boot_wait`}}",
      "communicator": "ssh",
      "ssh_private_key_file": "~/.vagrant.d/insecure_private_key",
      "ssh_username": "core",
      "ssh_password": "",
      "ssh_port": 22,
      "cpus": "{{ user `cpus` }}",
      "disable_shutdown": true,
      "guest_additions_mode": "attach",
      "guest_os_type": "RedHat_64",
      "iso_interface": "sata",
      "headless": "{{ user `headless` }}",
      "iso_checksum": "sha256:{{ user `iso_checksum` }}",
      "iso_url": "{{ user `iso_url` }}",
      "memory": "{{ user `memory` }}",
      "type": "virtualbox-iso",
      "virtualbox_version_file": ".vbox_version",
      "guest_additions_path": "VBoxGuestAdditions.iso",
      "vm_name": "{{user `vm_name`}}",
      "shutdown_command": "sudo -S shutdown -P now",
      "ssh_wait_timeout": "600s",
      "bundle_iso": true,
      "vboxmanage": [
        [ "modifyvm", "{{ .Name }}", "--natpf1", "guestssh,tcp,,2222,,22" ], [ "modifyvm", "{{ .Name }}", "--natpf1", "guesthttp,tcp,,8080,,80" ], [ "modifyvm", "{{ .Name }}", "--natpf1", "guesthttps,tcp,,8443,,443" ]
  ]}],
  "post-processors": [
    [
      {
        "compression_level": 9,
        "output": "box/{{user `vm_name`}}-{{user `version`}}.box",
        "type": "vagrant",
        "vagrantfile_template": "vagrantfile.tpl"
      },
      {
        "access_token": "{{user `access_token`}}",
        "box_tag": "{{user `box_tag`}}",
        "type": "vagrant-cloud",
        "version": "{{user `version`}}"
      }
    ]
  ],
  "variables": {
    "iso_checksum": "0100fc6e60ff4462137ae4ba9b6ed09d908b687f166ed6a4a054892c5796d4f6",
    "access_token": "{{env `VAGRANT_CLOUD_TOKEN`}}",
    "boot_wait": "30s",
    "box_tag": "sudo_killself/unicorni",
    "cpus": "2",
    "headless": "false",
    "iso_url": "/home/marioushka/liveISO/rhcos-live.x86_64.iso",
    "memory": "2000",
    "version": "4.9.0",
    "vm_name": "unicorni"
  }
}

